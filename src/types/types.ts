export enum AppTab {
  DASHBOARD = 'dashboard',
  LIFTING = 'lifting',
  FOOD = 'food',
  ANALYTICS = 'analytics',
}

export enum Macro {
  PROTEIN = 'protein',
  CARBS = 'carbs',
  FAT = 'fat',
}

export enum WorkoutType {
  A = 'A',
  B = 'B',
}

export enum Timeframe {
  SIX_MONTHS = '6M',
  ONE_YEAR = '1Y',
  ALL = 'ALL',
}

export enum SleepTimeType {
  BED = 'bed',
  WAKE = 'wake',
}
